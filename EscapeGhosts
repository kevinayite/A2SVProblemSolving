class Solution:
    def escapeGhosts(self, ghosts: List[List[int]], target: List[int]) -> bool:
        distance = abs(target[0]) + abs(target[1])
        for x, y in ghosts:
            if distance >= (abs(x-target[0]) + abs(target[1] - y)):
                return False
        return True
        
