class Solution:
    def restoreString(self, s: str, indices: List[int]) -> str:
        word = [""] * len(s)

        for i in range(len(s)):
            word[indices[i]] = s[i]

        return "".join(word)
