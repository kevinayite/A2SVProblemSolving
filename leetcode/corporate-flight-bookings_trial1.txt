class Solution:
    def corpFlightBookings(self, bookings: List[List[int]], n: int) -> List[int]:
        result = [0]*n
        for first, last, seats in bookings:
            result[first -1] += seats # as for our index start by 0
            if last < n:
                result[last] -= seats
        for i in range(1, n):
            result[i] += result[i-1]
        return result 

        