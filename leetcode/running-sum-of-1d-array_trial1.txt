class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        k = len(nums)
        result = []
        for i in range(1,k+1):
            result.append(sum(nums[:i]))
        return result

        